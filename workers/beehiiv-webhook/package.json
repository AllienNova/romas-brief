{
  "name": "@romas-brief/beehiiv-webhook",
  "private": true,
  "version": "0.0.0",
  "description": "SHIP-11 · Beehiiv -> Supabase subscriber-sync webhook. Shared-secret custom-header auth per ADR-0019 (no native Beehiiv HMAC).",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "lint": "echo \"(no eslint config in workers/* — typecheck is the gate)\" && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/sync.test.ts",
    "clean": "rimraf dist .wrangler .turbo"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3",
    "wrangler": "^3.95.0"
  }
}
