{
  "name": "@romas-brief/config",
  "private": true,
  "version": "0.0.0",
  "description": "Shared config presets — tsconfig, eslint, prettier. Consumed by apps + workers + packages.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tailwind": "./src/tailwind.ts"
  },
  "scripts": {
    "lint": "echo \"(M1 stub) lint lands in T-117\" && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo \"(M1 stub) tests land in T-117\" && exit 0",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "tailwindcss": "3.4.15",
    "typescript": "^5.6.3"
  }
}
