{
  "name": "@romas-brief/notice-scheduler",
  "private": true,
  "version": "0.0.0",
  "description": "NB-5 (§10). Cron every 5 min: call notices_run_scheduler() (promote scheduled→published, expire published→expired, hold one-live-featured) then revalidate the board tag.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "lint": "echo \"(stub) lint\" && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo \"(stub) tests — scheduler logic covered by supabase/tests/notices_scheduler.sql\" && exit 0",
    "clean": "rimraf dist .wrangler .turbo"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3",
    "wrangler": "^3.95.0"
  }
}
