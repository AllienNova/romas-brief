name = "romas-brief-beehiiv-webhook"
main = "src/index.ts"
compatibility_date = "2026-05-01"
compatibility_flags = ["nodejs_compat"]

# Reserved name — handler logic to be authored under T-310C-A..C (Phase 7).
# Until then, fetch() returns HTTP 501. No cron triggers.
# Secrets that will be required at activation:
#   BEEHIIV_WEBHOOK_SECRET (HMAC-SHA256 verification key)
#   SUPABASE_SERVICE_ROLE_KEY (subscriber state writes)

[vars]
SUPABASE_URL = "https://placeholder.supabase.co"
